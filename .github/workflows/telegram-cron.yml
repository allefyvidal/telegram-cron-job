name: 🤖 Mão invisível do mercado
on:
  schedule:
    - cron: '0 9,12,15,17 * * 1-5'  # 9h, 12h, 15h, 17h BRT
  workflow_dispatch:

jobs:
  executar-bot:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Baixar código
        uses: actions/checkout@v4
        
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: 📦 Instalar dependências
        run: pip install -r requirements.txt  # ⚠️ VERIFIQUE SE TEM ESTA LINHA!
        
      - name: 🔍 Executar bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python src/bot_main.py
